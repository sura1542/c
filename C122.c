#include<stdio.h>
#include<conio.h>

int main()
/*{
    float a = 12000;
    if (a > 10000)
    {
        a = a + (a*0.05);
        printf(" a = %d\n",a);

        else if (a < 10000)
        {
            a = a + (a*0.05);
            printf(" a = %d\n",a);
        }
        
    }
    
}

{
    float a,b,d;
    int c;
    printf("input a,b : ");
    c = a*b;
    d = a*b - c
    if (d >= 5)
    {
        printf("c = %d\n",c);
    }
return(0);
}*/

